docker:
  web: client/Dockerfile
release:
  image: web
  command:
    - echo 'Starting client server after deployment!';
    
  api: server/Dockerfile
release:
  image: api
  command:
    - echo 'Starting api server after deployment!';